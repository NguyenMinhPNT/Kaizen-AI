import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // Sprint 1 — infrastructure only. Full widget tests in Sprint 8.
    expect(true, isTrue);
  });
}
